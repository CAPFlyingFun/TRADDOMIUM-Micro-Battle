node_modules/
dist/
*.local
.DS_Store
probe-island.png
tsconfig.tsbuildinfo

# Scratch renders from headless checks
*.png
!public/*.png
# The art masters. bakeArt.mjs reads these to produce everything in
# public/ AND to measure the loading bar's cutout, so a clone without
# them cannot rebuild the splash.
!art/*.png
